# ALCOHOLIC - Design Document
<img width="642" height="642" alt="image" src="https://github.com/user-attachments/assets/f9146ea7-461e-4c30-a661-eae781c78e18" />

|||
| --- | --- |
| **Title** | **ALCOHOLIC** |
| **Student No** | **22212022** |
| **Name** | **이시영** |
| **E-mail** | **dder1998@naver.com** |

---

## Revision History

| Revision Date | Version # | Description | Author |
|---|---|---|---|
| 06/03/2026 | 1.00 | 초안 | 이시영 |

---

## Contents

1. Introduction
2. Class Diagram
3. Sequence Diagram
4. State Machine Diagram
5. Implementation Requirements
6. Glossary
7. References

---

## 1. Introduction

본 문서는 project - alcoholic의 design phase에 관한 문서이다.

design phase에서 중요하게 구현해야 할 점은

1. 회원, 비회원, 관리자의 역할을 구분해서 다른 기능을 사용할 수 있게 한다.
2. 사용자들이 웹 서비스를 이용함에 불편함이 없도록 서버와 데이터베이스를 구축한다.

---

## 2. Class Diagram
<img width="642" height="460" alt="image" src="https://github.com/user-attachments/assets/74b29c3c-3448-436c-be7f-7ea1c5014d53" />

> 아래는 class들에 대한 설명이다.

| Class Name | Explanation |
|---|---|
| **User** | 데이터베이스의 회원 데이터를 표현하는 JPA 엔티티 클래스다. `id(String)`: 회원의 고유 식별자이자 기본 키. `favorites(List)`: 회원이 즐겨찾기(좋아요)를 표시한 주류의 고유 ID 목록. DB에는 `bigint[]` 배열 형태로 저장된다. `role(String)`: 회원의 권한 등급을 표시하며, 기본값은 `USER`이다. 관리자 식별 시 `ADMIN` 값을 가지며, ADMIN일 경우 술의 추가가 가능하다. |
| **Drink** | `id(Long)`: 주류의 자동 생성 고유 식별 번호이자 기본 키(PK). `abvRange(Double)`: 주류의 알코올 도수(ABV, %) 정보. `scents, tastes, atmospheres(List<String>)`: 각각 주류의 향 특징 태그, 맛 특징 태그, 추천 음용 분위기 태그들의 목록. DB에는 `text[]` 배열 형태로 저장된다. |
| **UserDto** | 회원 정보 송수신 시 사용되는 요청/응답 스키마 클래스들을 묶어 관리하는 홀더 클래스. `SignUpRequest`: 회원가입 시 클라이언트가 작성해 보내는 데이터 바구니(id, nickname, password 포함). `LoginRequest`: 로그인 시 클라이언트가 검증용으로 보내는 데이터 바구니(id, password 포함). `Response`: 계정 작업 완료 후 클라이언트에 최종 응답하는 바구니(id, nickname, role 포함). 비밀번호 필드가 제외되어 있어 네트워크 상에서 정보가 유출되지 않게 방지한다. |
| **DrinkDto** | 주류 관리 시 사용되는 데이터 입력/검색 조건 규격들을 묶어 관리하는 홀더 클래스. `RecommendRequest`: 맞춤 술 추천 조건(희망 상한 가격, 희망 도수, 선호 당도 레벨, 선호 향/맛 태그 목록)을 담는 조건부 요청 객체. `CreateRequest`: 관리자가 새로운 주류를 데이터베이스에 수기 입력해 생성할 때 담는 폼 데이터 전송 객체. |
| **UserRepository** | 데이터베이스 회원 테이블(`/users`)에 접근해 조회 및 조작 작업을 수행하는 저장소 인터페이스. `existsById(String id)`: 회원가입 시 중복 아이디 검사. `save(User user)`: 신규 회원 저장 또는 즐겨찾기 목록 수정 후 DB 갱신. `findById(String id)`: 로그인 또는 특정 유저 세부 데이터 탐색. |
| **DrinkRepository** | 데이터베이스 주류 테이블(`drinks`)에 접근해 조회 및 조작 작업을 수행하는 저장소 인터페이스. `save(Drink drink)`: 신규 주류 데이터 DB 저장. `findById(Long id)`: 특정 주류 단 건 상세 탐색. `findAll()`: 전체 주류 목록 로드 또는 추천 알고리즘 필터링 대상 리스트 일괄 로드. `findAllById(Iterable ids)`: 유저 즐겨찾기 주류 ID 목록을 한 번에 DB에서 가져와 목록 구성. |
| **AuthService** | 사용자 회원가입 및 로그인 절차에 관한 세부 유효성 검증과 비즈니스 로직을 처리. `signup(SignUpRequest request)`: 중복 아이디 여부를 `UserRepository`를 통해 검증하고, 아이디가 `admin`인 경우 역할을 `ADMIN`으로 자동 승격하며, 가입 완료 시 응답 객체를 반환. `login(LoginRequest request)`: 유저 아이디와 패스워드가 DB 정보와 일치하는지 대조 검증하고, 성공 시 회원 세션을 위한 데이터 결과(`Response`)를 반환. |
| **DrinkService** | 주류 관리, 즐겨찾기 상태 판단 및 토글 작업, 취향 맞춤 주류 추천 핵심 로직을 계산. `getAllDrinks()`: 전체 주류 리스트 반환. `getDrinkById(Long id)`: 주류 단 건 조회, 미존재 시 예외 처리. `getUserFavorites(String userId)`: 유저 즐겨찾기 주류 ID를 수집해 상세 리스트 반환. `toggleFavorite(String userId, Long drinkId)`: 즐겨찾기 여부 판단 후 등록/삭제 토글. `isFavorite(String userId, Long drinkId)`: 특정 유저의 즐겨찾기 여부를 boolean으로 반환. `recommendDrinks(RecommendRequest request)`: 가격·도수 조건(오차 ±4.0%) 1차 필터링 후 태그 일치율 점수 기반 정렬 추천 리스트 반환. |
| **AuthController** | 사용자 계정 생성 및 로그인 API 엔드포인트를 노출하고 프론트엔드와 HTTP 신호를 조율. `signup(@RequestBody SignUpRequest request)`: `POST /api/auth/signup` 접수 후 `AuthService` 실행. `login(@RequestBody LoginRequest request)`: `POST /api/auth/login` 접수 후 회원 검증 및 성공 세션 반환. |
| **DrinkController** | 주류 데이터에 관한 모든 조회 및 조작 관련 REST API 엔드포인트를 노출. `getAllDrinks()`: `GET /api/drinks` - 전체 주류 배열 응답. `getDrinkById(@PathVariable Long id)`: `GET /api/drinks/{id}` - 특정 술 상세 내역 응답. `getUserFavorites(@RequestParam String userId)`: `GET /api/drinks/favorites` - 유저 즐겨찾기 술 리스트 응답. `toggleFavorite(@PathVariable Long id, @RequestParam String userId)`: `POST /api/drinks/{id}/favorite` - 찜하기 토글 처리. `isFavorite(@PathVariable Long id, @RequestParam String userId)`: `GET /api/drinks/{id}/is-favorite` - 즐겨찾기 여부 true/false 응답. |

---

## 3. Sequence Diagram

### 1. Sign up
<img width="642" height="263" alt="image" src="https://github.com/user-attachments/assets/85221bd8-1021-448f-a2fb-474090186a33" />

사용자가 회원 정보를 입력하고 가입을 시도하면, 이미 회원가입이 되어 있는 아이디인지 확인한 후 중복된 아이디가 존재한다면 이미 존재하는 아이디라는 에러 메시지를 출력한다. 그렇지 않고 가입 정보가 유효한 새로운 정보라면 DB에 회원 데이터를 저장한 뒤 가입 완료 메시지를 보내고 로그인 화면으로 사용자를 이동시킨다.

### 2. Log in
<img width="642" height="285" alt="image" src="https://github.com/user-attachments/assets/555bbc2f-15f2-445d-94f0-33d8f580d96b" />

로그인 화면에서 아이디와 비밀번호 정보를 전송하면 백엔드에서 회원 테이블 조회를 통해 가입 정보를 탐색한다. 존재하지 않는 계정이거나 비밀번호 검증이 실패하면 불일치 메시지 및 상태 에러를 띄운다. 검증을 통과한 경우에는 로그인 성공 유저 정보를 로컬 스토리지에 저장하고 메인 화면으로 리다이렉트한다.

### 3. Log out
<img width="642" height="335" alt="image" src="https://github.com/user-attachments/assets/bf359e7e-3446-4f2f-8090-a5975dc4f6fc" />

로그아웃을 호출하면 브라우저의 로컬 스토리지(`localStorage`)에 담긴 사용자 세션 및 인증 데이터를 완전히 소거한 후, 자동으로 메인 화면으로 화면을 갱신 및 이동시킨다.

### 4. Show Drink (전체 주류 목록 조회)
<img width="642" height="231" alt="image" src="https://github.com/user-attachments/assets/f6e10f9b-22f5-4ec4-a12f-c27fd900b01a" />

주류 목록 화면에 최초 진입(마운트)하면 자동으로 전체 목록 조회를 백엔드에 요청하여 전체 주류 데이터를 획득한다. 그 후 프론트엔드가 자체적인 페이징 연산을 통해 한 화면에 10개씩 술 리스트를 잘라내어 화면에 카드로 렌더링하고 페이징 번호 버튼을 구성한다.

### 5. Show Drink Detail (주류 상세 정보 조회)
<img width="642" height="214" alt="image" src="https://github.com/user-attachments/assets/701f0b55-75cb-4d4f-a47e-a14703b51c92" />

사용자가 주류 카드 등을 클릭하면 주류 ID 값을 매개변수로 상세 페이지로 이동하며 백엔드에 단 건 조회 요청을 보낸다. 서버가 DB에서 해당 ID와 일치하는 고유한 술 정보(설명, 맛 태그, 당도, 도수 등)를 성공적으로 수집하여 프론트엔드에 응답하면 해당 세부 데이터를 화면에 렌더링한다.

### 6. Search Drink by Name
<img width="642" height="253" alt="image" src="https://github.com/user-attachments/assets/91cb7dc3-8bfa-478c-96c6-ae7b2b0a440e" />

사용자가 특정 검색어를 제출하면, 프론트엔드는 백엔드에 요청해 전체 주류 목록 데이터를 얻어온다. 이후 프론트엔드는 메모리 상에서 입력된 검색어가 각 주류 이름에 포함(`includes`)되어 있는지를 판정하여 일치하는 주류 카드들만 화면에 구성한다.

### 7. Search Drink by Type (맞춤 주류 추천)
<img width="642" height="191" alt="image" src="https://github.com/user-attachments/assets/129804e8-add6-46f4-8561-5c6c02ac094d" />

사용자가 예산, 타겟 도수, 단맛 조건 및 맛/향 태그 설문지를 작성해 요청하면, 백엔드 필터링 엔진이 구동되어 가격과 도수의 허용 오차 조건을 판정한다. 그 뒤 맛/향 태그 일치도를 계산하여 가장 적합도가 높은 순서대로 리스트를 정렬해 클라이언트에 내려보내며, 화면에서는 이를 수집해 결과 창을 노출하고 스크롤 위치를 결과 창으로 부드럽게 이동시킨다.

### 8. Save Favorite Drink
<img width="642" height="236" alt="image" src="https://github.com/user-attachments/assets/00658c8d-fc4b-4733-abbc-ebab51e197ea" />

사용자가 빈 하트 단추를 클릭하면 백엔드에 찜하기를 요청한다. 백엔드는 해당하는 사용자의 정보를 가져온 뒤 해당 회원의 `favorites` 리스트에 요청된 주류 ID를 추가하여 DB를 업데이트한다. 성공 응답을 받아오면 화면의 하트를 가득 찬 붉은색 하트 이미지로 변경하여 즐겨찾기 저장을 시각화한다.

### 9. Delete Favorite Drink
<img width="642" height="224" alt="image" src="https://github.com/user-attachments/assets/abb2de85-9310-4fac-a9c4-88675c8919a8" />

사용자가 채워진 하트 혹은 즐겨찾기 목록 내의 제거 아이콘을 클릭하면 해제 처리를 요청한다. 백엔드에서 사용자 가입 정보 내의 `favorites` 목록을 읽고 삭제할 주류 ID만 제외시킨 다음 DB 유저 정보를 업데이트한다. 갱신된 사용자의 결과를 수신하면, 프론트엔드는 목록 화면에서 해당 주류 카드 아이템을 실시간 필터링을 통해 제거한다.

### 10. Show Favorite Drinks
<img width="642" height="198" alt="image" src="https://github.com/user-attachments/assets/6255bcf4-18fb-4d9f-8803-f3922a022d94" />

사용자가 즐겨찾기 페이지로 접속하면 자동으로 API 조회를 작동시키고, 백엔드에서 해당 유저 데이터에 기록되어 있는 주류 고유 ID 리스트 정보를 취득한다. 그 후 주류 ID에 포함되는 상세한 술 정보 행들을 DB에서 찾아 리스트 형식으로 반환하면, 프론트엔드가 이를 받아 즐겨찾는 술 정보 카드로 화면을 렌더링한다.

### 11. Create / Register New Drink
<img width="642" height="292" alt="image" src="https://github.com/user-attachments/assets/5f20d212-1cf4-479a-b0f0-90ba47f40a76" />

새로운 주류 스펙 정보를 제출하면, 백엔드 내부에서는 등록자가 시스템 관리자인지 권한 검증 절차를 거친다. 관리자 권한을 취득하지 못했거나 권한 검증에 예외가 발생하면 즉시 API 호출을 거부하고 홈 화면으로 강제 이동(리다이렉트)시킨다. 권한이 충족된다면 주류 저장 쿼리를 데이터베이스로 작동시켜 저장을 마친 뒤 성공 응답을 내려주고, 화면의 폼 입력 필드를 비우며 완료 메시지를 팝업한다.

---

## 4. State Machine Diagram

### 1. Client
<img width="632" height="833" alt="image" src="https://github.com/user-attachments/assets/75c93a5e-ef15-44a3-b98b-9259c9b121db" />

사용자가 최초 접속 시 **Guest(비로그인)** 상태로 시작한다. 사용자는 여기서 바로 로그인 창으로 전이하거나, 혹은 주류 조회/검색/추천을 처리하는 공통 서비스(**CommonServices**) 상태로 진입하여 탐색할 수 있다.

로그인 시도 시 LoginState에서 권한 식별 후 **Member(일반 회원)** 또는 **Admin(관리자)** 상태로 분기한다. 로그인 성공 이후에도 사용자는 대시보드 상태에 머무는 것 외에, 상시 공통 서비스로 전환하여 주류를 검색하거나 추천받는 기능을 수행할 수 있으며, 뒤로가기를 통해 대시보드로 안전하게 복귀한다. 로그아웃을 시도하면 즉시 권한을 잃고 **Guest** 상태로 환원된다.

### 2. Server
<img width="642" height="294" alt="image" src="https://github.com/user-attachments/assets/a6648845-5ef1-492f-8c18-748931036f9b" />

백엔드 서버 엔진이 최초로 구동된 후 클라이언트로부터 들어오는 데이터 트랜잭션 요청을 처리하는 내부 상태 라이프사이클을 나타낸다.

서버는 상시 요청 대기 상태(**Listening**)를 유지하다가, 들어오는 API의 종류에 따라 적절한 연산 상태로 분기한다.

| API 종류 | 전이 상태 |
|---|---|
| 회원가입 / 로그인 검증 | Auth (인증 상태) |
| 상품 탐색 | DBQuery (DB 쿼리 상태) |
| 취향 알고리즘 요청 | RecAlg (알고리즘 연산 상태) |
| 신규 제품 등록 | DBUpdate (DB 트랜잭션 갱신 상태) |

각 비즈니스 로직을 마친 후 성공 혹은 에러 상태코드 응답(**Responding**)을 클라이언트에 전송하고 나면 다음 요청을 즉시 수용할 수 있도록 대기 상태로 복귀하며, 백엔드 서비스 정지 시 리소스 회수 절차(**CloseServer**)를 수행한 뒤 최종 데몬 작동이 종료된다.

---

## 5. Implementation Requirements

### H/W Platform Requirements

| Item | Specification |
|---|---|
| Processor | Intel or AMD 6-core or higher CPU |
| Memory | 8GB RAM |
| Storage | 5GB Usable Space |

### S/W Platform Requirements

| Item | Specification |
|---|---|
| OS | Windows 10 or higher |
| Runtime Environment | JDK 17 or higher, Node.js LTS v18.x 이상, npm v9.x or higher |
| Database | PostgreSQL 15 or cloud Supabase PostgreSQL 원격 서버 접근 가능 상태 |
| Build Tools | Gradle 8.x or higher |

---

## 6. Glossary

| Term | Description |
|---|---|
| **tag (태그)** | 술이 가지고 있는 맛, 향, 분위기 등의 특성 |
| **즐겨찾기** | 사용자가 본인의 취향에 맞아 저장해둔 주류 목록 |
| **atmospheres (분위기)** | 술에 어울리는 상황이나 분위기. 예) 소주 → "삼겹살", "친구와" 등의 태그 |

---

## 7. References

<!-- 참고 문헌(도서, 논문, 기술 보고서 등)을 이곳에 기재하세요. -->
